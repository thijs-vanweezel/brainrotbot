# brainrotbot

A personal, single-user automation project. Not packaged for reuse — no support, no
installation guide, no guarantees.

## Run

```
run.bat
```

Runs from a terminal or by double-clicking. Extra arguments pass through. Requires the
local conda environment described in `environment.yml`.

## Tests

```powershell
pytest
```
