# Base conda environment. Heavier optional deps are installed via the pyproject extras.
name: brainrotbot
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - pip
  - requests>=2.31
  - python-dotenv>=1.0
  - pip:
      - pytest>=8.0
