# Committed template. Copy to .env (gitignored) and fill in real values there.
# Loaded automatically by config.load_settings(). Never put real secrets in this file.

PIXABAY_API_KEY=""
ZERNIO_API_KEY=""
