# Reddit retrieval needs NO credentials -- brainrotbot reads public RSS feeds.
#
# This file is reserved for secrets needed by later pipeline steps, e.g. the
# TikTok upload login (added in step 5). Copy to .env when those arrive.
#
# TIKTOK_USERNAME=
# TIKTOK_PASSWORD=
